{
  "name": "idle-state",
  "version": "1.0.0",
  "description": "tell if browser on idle state",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rollup -c",
    "dev": "rollup -c -w"
  },
  "keywords": [
    "browser",
    "idle",
    "state"
  ],
  "author": "cloud-cb",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "^7.14.6",
    "@babel/preset-env": "^7.14.7",
    "rollup": "^2.53.2",
    "rollup-plugin-babel": "^4.4.0"
  }
}
